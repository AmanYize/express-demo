{
  "name": "express-demo",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "start": "node app.js",
    "test": "npx mocha --exit --reporter mocha-junit-reporter --reporter-options mochaFile=test-results/test-results.xml"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "prom-client": "^14.2.0"
  },
  "devDependencies": {
    "mocha": "^10.2.0",
    "supertest": "^6.3.3",
    "mocha-junit-reporter": "^2.2.1"
  }
}